{
  "name": "remix-aeirmist-2-0",
  "private": true,
  "version": "2.0.0",
  "scripts": {
    "build": "cd imported_repo && npm run build",
    "build:pages": "cd imported_repo && npm run build:pages",
    "lint": "cd imported_repo && npm run lint",
    "cap:sync": "cd imported_repo && npm run build:native"
  },
  "dependencies": {
    "@capacitor/android": "^8.5.2",
    "@capacitor/cli": "^8.5.2",
    "@capacitor/core": "^8.5.2"
  }
}
